# MCP servers the harness connects to (JSON array of Streamable HTTP endpoints).
# Default already targets the cre8-mcp-ui server below, so this is optional.
SERVERS=["http://localhost:3001/mcp"]

# Ports the harness serves on (defaults shown).
# HOST_PORT=8080
# SANDBOX_PORT=8081
